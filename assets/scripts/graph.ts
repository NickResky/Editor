// ${requires}

export default class GraphClass {
    // ${properties}

    /**
     * Called on the scene starts.
     */
    public onStart(): void {
        // ${onStart}
    }

    /**
     * Called each frame.
     */
    public onUpdate(): void {
        // ${onUpdate}
    }
}
